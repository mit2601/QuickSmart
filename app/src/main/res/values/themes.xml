<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuickSmart" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="textview1" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/_22sdp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/textPrimary</item>

    </style>
    <style name="logintitle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/black</item>

    </style>
    <style name="Btn" parent="Widget.Material3.Button">
        <item name="android:layout_height">@dimen/_40sdp</item>

        <!-- Shape -->
        <item name="cornerRadius">@dimen/_28sdp</item>

        <!-- Text -->
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/white</item>

        <!-- Color -->
        <item name="backgroundTint">@color/colorPrimary</item>

        <!-- Elevation -->
        <item name="elevation">@dimen/_4sdp</item>

    </style>

    <style name="textview2" parent="Widget.AppCompat.TextView">
        <item name="android:layout_marginStart">@dimen/_120sdp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:layout_marginTop">@dimen/_3sdp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginBottom">@dimen/_3sdp</item>




    </style>

    <style name="Button_home_search">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeBottomLeft">@dimen/_20sdp</item>
        <item name="cornerSizeBottomRight">@dimen/_20sdp</item>
    </style>







    <style name="Theme.QuickSmart" parent="Base.Theme.QuickSmart" />
</resources>